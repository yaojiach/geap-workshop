# Pinned: mcp 2.0.0 removed the `mcp.server.fastmcp` module, so an unpinned install
# breaks main.py at import time and the Cloud Run container never binds to $PORT.
# Keep these in sync with the root requirements.txt.
mcp==1.28.1
uvicorn==0.50.2
starlette==1.3.1
sqlalchemy==2.0.51
pg8000==1.31.5
